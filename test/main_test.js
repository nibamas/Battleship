const expect = require('chai').expect;
//test suite -  block of unit tests closely related that taste the same funtion or similar parts of the code base
describe('Mocha', () => {
  //test spec(individual test)
  it('should run our tests using npm', () => {
    expect(true).to.be.ok;
  });
})
